// Copy this file to environments.ts and fill in your values.
// environments.ts is gitignored — never commit real credentials.
export const environment = {
  supabase: {
    url: 'https://YOUR_PROJECT.supabase.co',
    anonKey: 'YOUR_ANON_PUBLIC_KEY',
  },
  admin: { email: 'your-admin@email.com' },
};
